#!/usr/bin/env bash

## svn
export SVN_EDITOR=vim
export DEV_DIR="$HOME/dev"

if [[ $df_OS_UNIX = true ]]; then
	export GOPATH=~/.golang_default
else
	export GOPATH=C:/Users/fcastillo/.golang_default
fi

